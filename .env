DATABASE_URL=postgresql+asyncpg://postgres:admin@localhost:5432/bookly_db
JWT_SECRET=29d2156881887d847932bae2d7b47cef
JWT_ALGORITHM=HS256
REDIS_HOST=localhost
REDIS_PORT=6379
#This is a .env file containing environment variables for a FastAPI application. Here's what each variable does:
# Database Configuration
# DATABASE_URL=postgresql+asyncpg://postgres:admin@localhost:5432/bookly_db
# This is the PostgreSQL database connection string with the following components:
# postgresql+asyncpg: Database dialect + async driver
# postgresql: PostgreSQL database type
# asyncpg: Async Python driver for PostgreSQL
# postgres: Database username
# admin: Database password
# localhost: Database host (local machine)
# 5432: Database port (PostgreSQL default)
# bookly_db: Database name
#------------------------------------------------------------------------------------------------------------------------
# JWT Authentication Configuration
# JWT_SECRET=29d2156881887d847932bae2d7b47cef
# Secret key used to sign and verify JWT tokens
# 32-byte hexadecimal string for cryptographic security
# Should be kept secret and never exposed publicly
# Used for creating and validating user authentication tokens
# JWT_ALGORITHM=HS256
# Signing algorithm for JWT tokens
# HS256 = HMAC with SHA-256 hash
# Most common and secure algorithm for symmetric key signing
# Other options: HS512, RS256, RS512
#---------------------------------------------------------------------------------------------------------------------------
# Redis Configuration
# REDIS_HOST=localhost
# Redis server hostname/IP address
# localhost means Redis is running on the same machine
# REDIS_PORT=6379
# Redis server port number
# 6379 is the default Redis port
